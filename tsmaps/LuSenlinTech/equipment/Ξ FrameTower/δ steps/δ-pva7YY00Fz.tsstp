## Two Frame Tower

Good for small spaces with a compact footprint of 0.5m² and height of 1m — offering hybrid setups and yields of ~5-10kg/year across two stackable units. 

To get started with customisation, let's configure each of the frames in turn.

