Add code to add html to top and bottom of fragment guides.
ie folder doesn't end with _frags, and has a single html file, open file and add before hml and after html.
Then add a command to yarn that runs jekyll and then runs the above code.

Working on prior - add leafy greens and then see padding top from options - too big!!!
Then work out how variables are set and how they must be per branch
Then get them to print out
Then decide how to lay them out

